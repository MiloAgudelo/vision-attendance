# Normaliza los finales de línea a LF en todo el repositorio (Prettier usa `endOfLine: lf`
# y la integración continua corre en Linux).
* text=auto eol=lf

pnpm-lock.yaml linguist-generated=true -diff
